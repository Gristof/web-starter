<!DOCTYPE html>
<html lang="en">
  <head>
    {% include "partials/head.njk" %}
    {% if site.ENV == 'production' %}
      <!-- capture the CSS content as a Liquid variable -->
      {% set css %}{% include "stylesheets/app.compiled.css" %}{% endset %}
      <style>
        {{ css | safe }}
      </style>
    {% else %}
    <link rel="stylesheet" href="/static/app.compiled.css">
    {% endif %}
  </head>
  <body class="theme-light | bg-background-primary text-copy-primary font-body transition ease-linear | js-main">

    {# <label for="toogleMode" class="toggle | flex items-center cursor-pointer absolute lg:fixed right-0 top-0 mt-8 mr-8 lg:mr-12">
      <div class="relative">
        <input id="toogleMode" type="checkbox" class="hidden | js-toggle-mode"/>
        <div class="toggle__line | w-10 h-4 bg-yellow-400 rounded-full shadow-inner"></div>
        <div class="toggle__dot | absolute w-6 h-6 bg-gray-400 rounded-full shadow inset-y-0 left-0"></div>
      </div>
    </label> #}

    {{ content | safe }}
  </body>
  <script src="/static/app.bundled.js"></script>
</html>
